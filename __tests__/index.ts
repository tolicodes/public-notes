describe("noteInitializer", () => {
  it("should ask for name and type of note");
  it("should create a branch for the note with clean name");
  it("should create a PR on Github");
  it("should create a ${type}/${name}.md file");
});
